更新conda
conda update -n base -c defaults conda

改变Python版本(不建议使用)
sudo conda install python=3.6

新增一个py3.6的环境
conda info --envs
conda create --name py36 python=3.6
conda remove --name py36 --all
conda activate py36
conda deactivate

安装某个库
conda install openbabel

寻找某个库所有的channel并安装指定的版本
anaconda search -t conda openbabel
conda install --channel https://conda.anaconda.org/openbabel openbabel

更新某个库
pip install networkx --upgrade


this is an important flag

